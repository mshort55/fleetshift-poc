apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: fleetshift-grpc-route-cert-reader
  namespace: __NAMESPACE__
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["__TLS_SECRET_NAME__"]
    verbs: ["get", "list", "watch"]
